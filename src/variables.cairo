fn main() {
    let mut x = 5;
    println!("The value of x is: {}", x);
    x = 6;
    println!("The new value of x is: {}", x);

    // const ONE_DAY_IN_SECS: u32 = 2600;
    // println!("Computational const is: {}", ONE_DAY_IN_SECS); 
    // const ONE_DAY_IN_SECS: u32 = (3600 * 24);

    // struct AnyStruct {
    //     a: u256,
    //     b: u32,
    // }

    // const ONE_HOUR_IN_SECS: u32 = 3600;

    //this is to practice computational constants
    // 
}